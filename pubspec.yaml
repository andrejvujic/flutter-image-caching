name: image_caching
description: An example on how to cache images from network.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  hive: ^2.1.0
  hive_flutter: ^1.1.0
  path_provider: ^2.0.4
  filesize: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
